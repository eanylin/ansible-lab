---
- name: Remove Nodes
  bigip_node:
    provider: "{{ f5_provider }}"
    name: "{{ hostvars[item].inventory_hostname }}"
    state: absent
    validate_certs: "no"
  loop: "{{ webapp_server }}"
  delegate_to: localhost
