---
- hosts: localhost
  connection: local
  become: false
  gather_facts: false

  tasks:
#    - name: Parse CSV file
#      command: python csv_to_yaml.py

#    - name: Include vars
#      include_vars: 
#        file: /tmp/output.yaml
#        name: output

    - name: Parse CSV File
      csv_to_yaml:
        name: files/acl_requirements.csv
      register: csv_parse_results

    - debug:
        msg: "{{ csv_parse_results['yaml_output']['routers'][0]['services'].split(',') }}"
