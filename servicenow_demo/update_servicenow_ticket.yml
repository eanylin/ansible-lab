---
- name: Update ServiceNow Ticket
  hosts: localhost
  gather_facts: false

  tasks:
  - name: Update ServiceNow Ticket
    include_role:
      name: update_snow_ticket
      vars_from: "password.yml"
